<IfModule mod_rewrite.c>
    RewriteEngine On
    # Forward all requests to public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Deny direct access to sensitive files if above fails
<FilesMatch "^\.env">
    Require all denied
</FilesMatch>
